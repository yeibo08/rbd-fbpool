# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project

`rbd-fbpool` is a Node.js project in its earliest stage — only a `.gitignore` exists so far. Update this file as the project takes shape.

## Stack

Node.js (inferred from `.gitignore`). Package manager, framework, and tooling are not yet decided.

## Commands

Add build, test, and lint commands here once `package.json` is created.
